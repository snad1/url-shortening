version: "3.7"
services:
  server1:
    container_name: server1
    restart: always
    build: server
    ports:
    - 5000:5000
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    volumes:
    - ./data:/data/db
    ports:
    - 27017:27017